import * as c from './src/common/expressions'
Hello "world"
Open "file.txt"


import * as c from './src/common/expressions'

let $COMMON_CONTEXT = c.initContext();
let $RESULT = null;

$RESULT = await Hello(["world"]);

$RESULT = await c.Open(["file.txt"]);

==========================================================

await browser.Run(ctx,"page.goto",["http://www.google.com.sg"],{})

browser.goto "https://www.google.com.sg"
